# Agent Notes

- Keep runtime changes in `src/`, `examples/`, and `tests/` separate from shared tooling maintenance.
- Shared configs, scripts, TypeDoc plugins, and dependency policy live in `../textmode-tooling`.
- Run `npm run check` before handing off changes.
